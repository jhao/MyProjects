# 08-SD-coding 日志

- 输入：用户指定前端 HTML/CSS/JS，后端 Python + Flask + SQLite，并要求保留切换 MySQL 能力，实现并验证功能可用。
- 输出：
  - Flask 应用结构：`app/__init__.py`、`app/config.py`、`app/db.py`、`app/routes.py`、`run.py`
  - 前端模板与静态资源：`app/templates/*.html`、`app/static/css/app.css`、`app/static/js/*.js`
  - 测试：`tests/test_db.py`、`tests/test_flask_app.py`
  - 启动说明：`README.md`
- 验证：
  - 当前环境没有 Flask 且网络受限，无法安装依赖并启动 Web 服务。
  - 已运行 SQLite 数据库层测试，覆盖用户种子数据、密码校验、项目默认空间、项目相关表写入。
  - 已运行 Python 语法编译检查。
- 追加调整：
  - 项目详情的里程碑页支持新增、编辑、删除和拖拽排序。
  - 新增里程碑输入项调整为单行布局。
  - 后端新增里程碑批量排序接口。
  - 项目日志页调整为左侧月历/检索、右侧日志编辑结构。
  - 月历按日志状态展示绿色、黄色、灰色点位，支持点击日期切换日志。
  - 日志编辑去掉日期输入，改为本地富文本编辑器；保存按钮按标题/内容变化自动启用。
  - 后端日志列表支持关键词检索。
  - 项目文档页调整为文件浏览器结构，左侧目录树支持展开/收缩、点击切换、新建、移动和删除。
  - 文档列表按当前目录过滤，文件支持预览弹层、下载源文件、移动目录和删除。
  - 文档目录树增加虚拟根目录；未归属任何文件夹的文件只显示在根目录下，所有文件夹默认挂在根目录下。
  - 修复文件移动接口：新增 `POST /api/documents/<id>/move` 兼容接口，前端改用 POST 移动文件，避免 PUT 返回 405。
  - 上传文件后等待文档列表刷新完成，确保目录切换后的文件列表立即更新。
  - 再次加固文件移动：新增项目内移动接口 `POST /api/projects/<project_id>/documents/<doc_id>/move`，前端改用项目内接口。
  - 目录树点击改为事件委托，确保目录切换后右侧文件列表刷新。
  - 项目编辑页下一步节点改为从项目里程碑中选择，未选择时留空。
  - 项目列表增加最新日志列，展示纯文本前 20 字，鼠标悬停显示全文浮层。
  - 统计信息移动到检索条件上方；启动日期范围和最后更新范围调整到同一行。
  - 筛选条件改为点击检索按钮后生效；新增导出按钮，导出当前检索结果为 Excel 可打开的 `.xls` 文件。
  - 项目列表冻结/启动操作按冻结状态互斥显示。
  - 分页上一页/下一页按当前页和总页数禁用。
  - 每页条数保存到 localStorage 和 cookie，进入页面时优先读取用户上次选择。
  - 登录页移除演示账号信息，取消默认账号/密码输入。
  - 登录增加图形验证码；注册和忘记密码使用真实 SVG 图形验证码，点击图片可刷新。
  - 邮箱验证码改为随机生成；配置 SMTP 时发送邮件，未配置时写入 `data/dev_mailbox.log`。
  - 增加忘记密码流程：邮箱、图形验证码、邮箱验证码、新密码。
  - 管理员账号改为启动参数或环境变量配置，默认 `admin` / `Ad123654`，并从后台用户管理列表隐藏。
  - 项目相关人简化为姓名、单位职务、备注，并支持一行式增删改查。
  - 日志日历支持点击年月弹出年月选择器；富文本编辑器增加字体、字号、颜色、格式刷、清除格式和源码切换。
  - 项目状态默认增加“未签合同”“招投标中”，数据库初始化会为现有普通用户补齐缺失默认状态。
  - 管理员界面增加项目状态维护，可新增、编辑状态名称、颜色、类型、排序和启用状态，并同步到所有普通用户。
  - 项目维度增加合同额、已开票金额、已回款金额；项目列表合并为一列展示，列排序按已回款金额数值排序。
  - 项目新建/编辑、列表接口、导出和 SQLite 旧库迁移同步支持三个金额字段。
